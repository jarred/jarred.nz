const plugin = require('tailwindcss/plugin');
const postcss = require("postcss");

const Prism = plugin(({ addVariant, addUtilities }) => {
  // const utils = [];
  // addUtilities(utils)
})



module.exports = Prism;